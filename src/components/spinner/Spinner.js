import React from 'react';
import './style.css';

const Spinner = () => {
  return (
    <div className="alignItem">
      <div className="loading-spinner" />
    </div>
  );
};

export default Spinner;
